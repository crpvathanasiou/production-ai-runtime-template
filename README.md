# fastapi-prod-starter

FastAPI starter showing an end-to-end path from local development to production-style Docker/Compose.

## Requirements

- Python 3.11
- Poetry
- Docker + Docker Compose (Docker Desktop on Windows/macOS)

## Local setup (Windows PowerShell)

1) Install dependencies:

```powershell
poetry config virtualenvs.in-project true
poetry install
